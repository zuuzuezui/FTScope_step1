# X-Bot / Twitter Bot

Bot Python pour scraper les tweets d’onglet Following et republier après reformulation IA.

## Installation

1. Copier `.env.example` en `.env` et remplir les valeurs.
2. Installer les dépendances:
```bash
pip install -r requirements.txt
